package com.$organisation;format="lower"$.$project;format="lower"$.service

import com.$organisation;format="lower"$.$project;format="lower"$.SpecKit

class MainSpec extends SpecKit {
  // Stub test
  "The main entrypoint" should "work" in {
    Main.main(Array.empty)
  }
}
